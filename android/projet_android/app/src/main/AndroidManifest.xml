<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/naofood_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/naofood_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySAE"
        tools:targetApi="31">
        <activity
            android:name=".controller.ResultActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".controller.SearchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.RecipeActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".controller.SettingsActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>